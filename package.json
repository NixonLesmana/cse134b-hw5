{
    "name": "nixon-hw5",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "eleventy --serve",
        "start": "eleventy --serve",
        "build": "eleventy && pagefind --site _site"
    },
    "devDependencies": {
        "@11ty/eleventy": "^3.1.2",
        "pagefind": "^1.5.2"
    }
}